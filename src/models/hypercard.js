const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const hyperCard = new Schema({
	cardID: {
		type: String,
		required: true,
	},
	info: {
		nick: {
			
		},
		
	},
	data: {
		
	},
	hide: {
		
	},
})
